for (let i = 1; i <= 100; i++) {
  let out = '';
  if (i % 3 === 0) out += 'Fizz';
  if (i % 5 === 0) out += 'Buzz';
  console.log(out || i);
}
/**  Весь код целиком
for (let i = 1; i <= 100; i++) {
  let out = '';
  if (i % 3 === 0) out += 'Fizz';
  if (i % 5 === 0) out += 'Buzz';
  console.log(out || i);
}

1️⃣ Цикл for
for (let i = 1; i <= 100; i++) {

Что здесь происходит:

let i = 1 — создаём переменную i и присваиваем ей 1

i <= 100 — цикл будет выполняться, пока i меньше или равно 100

i++ — после каждой итерации увеличиваем i на 1

📌 То есть цикл пройдёт по всем числам:
1, 2, 3, ..., 100

2️⃣ Переменная out
let out = '';


Создаётся строковая переменная out

Изначально она пустая строка ''

Она нужна, чтобы собрать результат для текущего числа.

3️⃣ Проверка на делимость на 3
if (i % 3 === 0) out += 'Fizz';

Разберём подробно:

i % 3 — оператор % возвращает остаток от деления

i % 3 === 0 — значит, число делится на 3 без остатка

Если условие истинно:

out += 'Fizz';


Это то же самое, что:

out = out + 'Fizz';


📌 Теперь out содержит "Fizz"

Пример:

i = 3 → 3 % 3 === 0 → out = "Fizz"

i = 6 → 6 % 3 === 0 → out = "Fizz"

4️⃣ Проверка на делимость на 5
if (i % 5 === 0) out += 'Buzz';


Работает аналогично, но для числа 5.

Если число делится на 5:

к out добавляется строка "Buzz"

Примеры:
i	Что происходит с out
5	"" → "Buzz"
10	"" → "Buzz"
15	"Fizz" → "FizzBuzz"

📌 Ключевой момент:
Оба if выполняются независимо, поэтому при i = 15:

out = ''      // начало
out += 'Fizz'
out += 'Buzz'
out === 'FizzBuzz'

5️⃣ Вывод результата
console.log(out || i);


Это самая «хитрая» строка. Разберём её медленно.

Оператор || (логическое ИЛИ)

В JavaScript || возвращает первое "truthy" значение, а не просто true или false.

Значение	Truthy / Falsy
'' (пустая строка)	❌ falsy
'Fizz'	✅ truthy
'Buzz'	✅ truthy
'FizzBuzz'	✅ truthy
любое число ≠ 0	✅ truthy
Как работает out || i

Если out не пустая строка → выводится out

Если out === '' → выводится i

Примеры:
i = 1
out === ''
console.log('' || 1) → 1

i = 3
out === 'Fizz'
console.log('Fizz' || 3) → 'Fizz'

i = 5
out === 'Buzz'
console.log('Buzz' || 5) → 'Buzz'

i = 15
out === 'FizzBuzz'
console.log('FizzBuzz' || 15) → 'FizzBuzz'

6️⃣ Итоговая логика

Для каждого числа от 1 до 100:

Создаём пустую строку

Если делится на 3 → добавляем "Fizz"

Если делится на 5 → добавляем "Buzz"

Если ничего не добавили → выводим число

Если добавили → выводим строку

🧠 Почему это решение хорошее

✅ Нет вложенных if
✅ Нет дублирования условий
✅ Код короткий и читаемый
✅ Легко расширяется (например, на 7 → "Pop") */
